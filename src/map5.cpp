#include<bits/stdc++.h>
using namespace std;
#define optimize()ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
int main()
{
    optimize();
    map<int,string>name;
    name[1] = "Sumon";
    name[1] = "opi";//assign is possible in maping so the answer will 'opi'
    name[2] = "Habiba";
    name[3] = "Alamin";
    cout<<name[1]<<endl;
}

