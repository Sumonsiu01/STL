#include<bits/stdc++.h>
using namespace std;
#define optimize()ios_base::sync_with_stdio(0);cin.tie(0);cout.tie(0);
int main()
{
    optimize();
    pair<int,int>p;
    p = make_pair(2,3);
    p.first++;
    cout<<p.first<<endl;
    cout<<p.second<<endl;
}
